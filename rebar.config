{erl_opts, [debug_info]}.

{deps, [
    {epgsql, "4.7.1"}
]}.

{profiles, [
    {test, [
        {erl_opts, [debug_info]}
    ]}
]}.

%% escript for the `winn` CLI tool
{escript_main_app, winn}.
{escript_name, winn}.
{escript_emu_args, "%%! -escript main winn_cli\n"}.
